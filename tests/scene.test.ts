import { describe, expect, it } from 'vitest'; import { createInitialDefenders, getFieldLayout } from '../src/game/layout';
describe('field layout', () => { it('creates three defenders inside the portrait field', () => { const defenders = createInitialDefenders(390, 844); expect(defenders).toHaveLength(3); defenders.forEach(d => { expect(d.center.x).toBeGreaterThan(d.radius); expect(d.center.x).toBeLessThan(390 - d.radius); }); }); it('keeps goal above ball and goalkeeper', () => { const field = getFieldLayout(390, 844); expect(field.goal.y + field.goal.height).toBeLessThan(field.ball.y); expect(field.goalkeeper.center.y).toBeLessThan(field.ball.y); }); });
